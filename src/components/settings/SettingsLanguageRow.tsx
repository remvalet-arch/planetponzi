"use client"; import { useEffect, useState } from "react";
import { motion } from "framer-motion"; import type { Locale } from "@/src/lib/i18n/strings"; const STORAGE_KEY = "pp-locale"; export function SettingsLanguageRow() { const [locale, setLocale] = useState<Locale>("fr"); useEffect(() => { try { const raw = localStorage.getItem(STORAGE_KEY); if (raw === "en"|| raw === "fr") setLocale(raw); } catch { /* ignore */ } }, []); const apply = (next: Locale) => { setLocale(next); try { localStorage.setItem(STORAGE_KEY, next); } catch { /* ignore */ } }; return ( <div className="flex w-full max-w-sm flex-col gap-2 pp-panel-sci rounded-pp-xl p-4"> <p className="font-mono text-[10px] uppercase tracking-widest text-slate-500">Langue</p> <div className="flex gap-2"> {(["fr", "en"] as const).map((loc) => ( <motion.button key={loc} type="button" whileTap={{ scale: 0.96 }} onClick={() => apply(loc)} className={`flex-1 rounded-pp-lg border px-3 py-2 font-mono text-xs font-semibold transition-colors ${ locale === loc ? "border-cyan-500/50 bg-[#15161E] text-cyan-300": "border-white/10 bg-[#15161E] text-slate-400 hover:border-cyan-400/55 hover:shadow-[0_0_10px_rgba(6,182,212,0.2)]"}`} > {loc === "fr"? "Français": "English"} </motion.button> ))} </div> </div> );
}
